[project]
name = "instantmesh"
version = "0.0.1"
requires-python = ">=3.10"

dependencies = [
"nvdiffrast @ git+https://github.com/NVlabs/nvdiffrast.git"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["*"]
exclude = ["assets*", "examples*", "outputs*"]
